import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow Yahoo Finance API calls from server-side routes
  // No special config needed since this is server-to-server
};

export default nextConfig;
